/bin/
/data/
coverage.out
coverage.raw
coverage.html
*.db
*.db-wal
*.db-shm
.DS_Store

# runtime task data (durability lives in the private agenttasks-data repo)
.beads/

# built CLI binary (use `go install ./cmd/tasks`)
/tasks
